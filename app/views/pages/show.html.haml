- title "Pages"

%p
  %strong Name:
  = @page.name

%p
  = link_to "Edit", edit_page_path(@page)
  |
  = link_to "Destroy", @pages, :confirm => 'Are you sure?', :method => :delete
  |
  = link_to "View All", pages_path
